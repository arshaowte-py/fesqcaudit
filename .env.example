# Optional: Vercel KV (recommended for production deploys)
# If these are not set, the app automatically falls back to local file storage
# at data/audits.json (great for local development).
KV_REST_API_URL=
KV_REST_API_TOKEN=
